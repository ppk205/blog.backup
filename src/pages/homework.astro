---
import type { HomeworkEntry } from "@/types";
import EntryLayout from "@components/homework/EntryLayout.astro";
import { getIndex } from "@lib/contentParser";

const entry = (await getIndex("homework")) as HomeworkEntry;
---

<EntryLayout entry={entry} />
